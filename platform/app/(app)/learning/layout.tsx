import { LearningShell } from '@/components/modules/learning/learning-ui'

export default function LearningModuleLayout({ children }: { children: React.ReactNode }) {
  return <LearningShell>{children}</LearningShell>
}
