# Vercel / Render frontend — REQUIRED (server-side proxy to Django):
BACKEND_URL=https://ibbul-acadamicos.onrender.com

# Do NOT set NEXT_PUBLIC_API_URL to the backend on Vercel — the browser uses /api/backend proxy.
# Local dev only:
NEXT_PUBLIC_API_URL=http://localhost:8000

NEXT_PUBLIC_APP_NAME=IBBUL Academic Platform
NEXT_PUBLIC_APP_SHORT_NAME=IBBUL
